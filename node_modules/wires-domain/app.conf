app.port = 3000

domain.adapter.type = 'file'

domain.adapter.opts
{
	host 		: 'localhost'
	user		: 'root'
	password	: ''
	database	: 'domain'
	sync 		: [ $domain.test.models ]
}

#domain.setupRootUser = true